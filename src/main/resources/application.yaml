spring:
  application:
    name: toffCo
  threads:
    virtual:
      enabled: true
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5435/toffco_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:admin123}
    driver-class-name: org.postgresql.Driver
  docker:
    compose:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  mail:
    host: sandbox.smtp.mailtrap.io #smtp.gmail.com
    port: 587
    username: d2fa3ae3acd9ab #walterro465@gmail.com
    password: 01a5d89028dc2f
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  data:
    redis:
      host: localhost
      port: 6380

  cache:
    type: redis

jwt:
  secret: ${JWT_SECRET:suaSenha}

server:
  port: 8081

evolution:
  api:
    url: http://localhost:8080
    instance: ToffCoBot
    key: senha_secreta_23C_ads_Te